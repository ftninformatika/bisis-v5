<html> 
<head>
    <meta http-equiv="Content-Type"  content="text/html charset=UTF-8" />
</head>
<body>
    <p>${dear}</p>
    <p>${body1}</p>
    <p>${body2} <a href="${activationLink}">${here}</a>.</p>
    <p>${catalogue} <a href="${opacLibAddr}">${webaddr}</a>.</p>
    <p>${body3}</p>
    <br>
    <p>${libraryFullName}</p>
    <p>${locationAddress}</p>
    <p>${locationZip} ${locationCity}</p>
    <p><a href="${websiteUrl}">${websiteUrl}</a></p>
</body> 
</html>
